# Dagster Cloud Run Executor Example

This example demonstrates how to use the Cloud Run executor to run Dagster ops as Cloud Run jobs.

## Prerequisites

1. A GCP project with Cloud Run API enabled
2. Docker installed locally
3. Access to a container registry (GCR)
4. Python 3.9+
5. GCP credentials configured

## Setup

1. Install dependencies: 